<!DOCTYPE html>
<html>
  <head>
    <title>CodetestApiDigestAuthentication</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= yield %>

    <%- if Current.user.present? -%>
      <p>Logged in as <%= Current.user.email %> (<%= link_to 'logout', session_path, data: { "turbo-method": :delete } %>)</p>
    <%- end -%>
  </body>
</html>
